{
  "name": "multi-company-portal",
  "private": true,
  "version": "1.6.41",
  "type": "module",
  "description": "Multi-company login portal that hosts the FinTrack financial app per tenant, with master/staff accounts.",
  "whatsNew": [
    "Accessibility: a clear gold outline now shows where you are when you move around with the keyboard (Tab), across the whole app.",
    "Stat cards can be opened with the keyboard — Tab to one and press Enter — and the small edit/delete buttons are easier to tap on phones."
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.35.2",
    "vite": "^5.4.11"
  }
}
