{
  "name": "multi-company-portal",
  "private": true,
  "version": "2.1.9",
  "type": "module",
  "description": "Multi-company login portal that hosts the FinTrack financial app per tenant, with master/staff accounts.",
  "whatsNew": [
    "Dashboard: the stat cards now sit in two even rows of six (no lopsided last row) now that there are twelve of them.",
    "Gave the new \"Bank Blocked\" card a muted steel-blue so it no longer blends in with the other blue cards."
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.35.2",
    "vite": "^5.4.11"
  }
}
